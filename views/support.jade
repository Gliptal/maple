extends /views/common/layout.jade

block css
  link(rel="stylesheet", href="/style/support.css")

block content
  each entry, index in support
    row(id="support_row-#{index}")
      .hidden
      p.hidden!= entry

block script
  script(type="text/javascript" src="/javascripts/support.js")